# Base URL of the MindBridge backend API.
# Local dev default; override per environment.
VITE_API_URL=http://localhost:5000/api

# Google OAuth Web Client ID (same value as Backend GOOGLE_CLIENT_ID).
# Client IDs are public — safe to expose in the browser.
VITE_GOOGLE_CLIENT_ID=
